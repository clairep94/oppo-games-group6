import React, { useState, useEffect, useRef, useCallback } from "react";
import getSessionUserID from "../../utility/getSessionUserID";

const Battleships = ({ gameId }) => {
  return (
    <div className="rock-paper-scissors">
      <h2>Battleships</h2>
    </div>
  );
};


export default Battleships;
